/// Rutas de assets centralizadas (launcher e imagen en app).
class AppAssets {
  AppAssets._();

  /// Mismo PNG que usa `flutter_launcher_icons` para ícono del sistema.
  static const String appIcon = 'assets/icons/app_icon.png';
}
